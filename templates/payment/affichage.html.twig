{% extends 'baseUser.html.twig' %}

{% block title %}Hello ProduitController!{% endblock %}

{% block body %}

    <div class="row" style="display:flex;align-items:center;justify-content:center;margin-top:150px">
        <div class="card">
            <div class="card-body">
                <a href="{{ path('addProduit') }}"><i class="ri-arrow-left-circle-fill" style="font-size: 50px;"></i></a>
                <h5 class="card-title">Liste des Produit</h5>
                <table class="table table-striped">
                    <thead>
                    <tr>
                        <th>numéro facture</th>
                        <th>quantite</th>
                        <th>nom</th>
                        <th>prenom</th>
                        <th>prix total</th>

                    </tr>
                    </thead>
                    <tbody>
                    {% for payment in b %}
                        <tr>
                            <td>{{ payment.numfac }}</td>
                            <td>{{ payment.quantite }}</td>
                            <td>{{ payment.nom }}</td>
                            <td>{{ payment.prenom }}</td>
                            <td>{{ payment.prixtot }}</td>




                        </tr>



                    {% endfor %}

                    <td>
                        <a  class="btn  btn-dark"  href="{{ path('order') }}">Passer Commonde</a>
                    </td>

                    </tbody>
                </table>
            </div>


        </div>
    </div>
{% endblock %}

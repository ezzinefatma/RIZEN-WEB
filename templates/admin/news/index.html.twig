{% extends 'admin/index.html.twig' %}

{%  block title%}Hello EventController!{% endblock %}

{% block bodyy %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <table border="2">
        <thead>
        <th> Id news</th>
        <th> Titre news</th>
        <th>  Contenu</th>
        <th> Modification</th>
        <th> Suppression</th>
        </thead>
        <tbody>
        {% for news in n %}
            <tr>
                <td>{{news.idNews}}</td>
                <td>{{news.titreNews}}</td>
                <td>{{news.contenuNews}}</td>
                <td>
                    <a class="btn btn-success" href="{{ path('ModifierNews',{'idNews':news.idNews}) }}"> Modifier</a>

                </td>
                <td>
                <a class="btn btn-success" href="{{ path('DeleteNews',{'idNews':news.idNews}) }}"> Supprimer</a>
                </td>

            </tr>
        {% endfor %}
        </tbody>
    </table>

</div>
{% endblock %}
